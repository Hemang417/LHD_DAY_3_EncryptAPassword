<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>EncryptAPassword</title>
    <style>
        body{
            width: 100vw;
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            background-color: black;
        }
        form{
            width: 250px;
            height: 300px;
            background-color: rgba(255,255,255,0.15);
            display: flex;
            justify-content: center;
            align-items: center;
        }
        #pass{
            border-top: 0;
            border-left: 0;
            border-right: 0;
            border-bottom: 1px solid lightgray;
            background-color: transparent;
        }
    </style>
</head>
<body>
    <form action="/hashPass" method="post">
        <input type="password" name="pass" id="pass" placeholder="Enter a Password"><br>
        <input type="submit" value="Generate Hash">
    </form>
</body>
</html>